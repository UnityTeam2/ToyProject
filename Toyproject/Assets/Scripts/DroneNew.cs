using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DroneNew : MonoBehaviour
{/*
    public GameObject prefab;
    public Transform spawnPoint;
    public int maxInstances = 5;
    public float spawnInterval = 1f;

    private int instanceCount = 0; // 현재 생성된 객체의 수를 추적하기 위한 변수

    private void Start()
    {
        StartCoroutine(SpawnDrones());
        StopCoroutine(StopDrones());
    }

    private IEnumerator SpawnDrones()
    {
        while (instanceCount < maxInstances)
        {
            GameObject instance = Instantiate(prefab, spawnPoint.position, Quaternion.identity);
            instance.transform.SetParent(spawnPoint);
            instanceCount++;
            yield return new WaitForSeconds(spawnInterval);
        }
    }

    private IEnumerator StopDrones()
    {
        while(5<maxInstances)
        {
            yield return 0;
        }
    }
    */
}
